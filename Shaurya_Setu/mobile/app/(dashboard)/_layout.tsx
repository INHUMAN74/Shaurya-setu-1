import { Slot } from "expo-router";

/** Route group `(dashboard)` — URLs omit this segment; see `dashboard/_layout.tsx` for the stack. */
export default function DashboardGroupLayout() {
	return <Slot />;
}
